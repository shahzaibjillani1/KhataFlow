import { LocalizedTextPipe } from './localized-text-pipe';

describe('LocalizedTextPipe', () => {
  it('create an instance', () => {
    const pipe = new LocalizedTextPipe();
    expect(pipe).toBeTruthy();
  });
});
